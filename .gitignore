/target

# stupid things that can't help themselves but to pollute the repo
.idea/
.DS_Store
dht.db

# Tauri build output
/gui/src-tauri/gen

# downloaded media must never be committed again
*.mkv
*.mp4
*.iso
